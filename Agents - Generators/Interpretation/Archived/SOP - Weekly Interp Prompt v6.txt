WEEK: [Date Range]
ATHLETE: [Name]

Analyze the above weekly packet and original athlete intake snapshot using official VirtusFocus systems and definitions.

============================================================
BASELINE CONTINUITY REQUIREMENT (DO NOT SKIP)
============================================================

If an Athlete Snapshot (Baseline Profile) is included in the packet,
you MUST extract baseline anchors and carry them into JSON under:
baseline_profile

You will then evaluate weekly behavior relative to baseline under:
baseline_alignment

If no snapshot is provided:
Set baseline_profile and baseline_alignment fields to "insufficient data".
Do not infer baseline.

============================================================
WEEKLY DATA EXTRACTION RULES
============================================================

You must extract all Win the Day information directly from the Weekly Recap.

If previous week key outputs (up to 3 prior weeks) are supplied,
you MUST perform longitudinal comparison using those outputs.

IMPORTANT WEEKLY FORMAT RULE:
Weekly recaps are 7 days long.
There is no recorded day after Sunday.
Recommitment can ONLY be evaluated when a Missed day has a later day inside the same recap.
If the only Missed day is the final day of the week,
set recommitment_signal to "insufficient data".
Do not infer next-week Monday behavior.

If recap includes daily category ranges (Win / Neutral / Missed), calculate:
- win_days
- neutral_days
- missed_days
- weekly_tier
- consistency_profile
- recommitment_signal (per weekly format rule above)

If exact numeric scores (0–5) are provided,
use official scoring bands to determine daily categories and weekly tier.

Do not assume missing days.
If data is incomplete, mark the relevant field as "insufficient data".

============================================================
LONGITUDINAL REQUIREMENTS (TREND COMPARISON)
============================================================

When prior interpretation JSON outputs are provided:

- Compare weekly_tier direction (Improving | Stable | Declining)
- Compare growth_phase movement (Advancing | Stable | Regressing)
- Evaluate consistency_profile trend (Strengthening | Volatile | Stagnant)
- Evaluate recommitment_signal trend (Escalating | Stable | Weakening)
- Detect core_pattern continuity or shift
- Detect behavioral_root_cause stability or change
- Detect most_important_controllable evolution

If insufficient historical data exists:
Mark trend fields as "insufficient data".
Do not speculate.

============================================================
LONGITUDINAL METRICS RULES (NEW – DETERMINISTIC COMPUTATION)
============================================================

If 2–4 weeks of prior JSON are provided, compute:

- rolling_win_rate_4wk = (sum of win_days across available weeks) / (7 * weeks_available)

- rolling_missed_rate_4wk = (sum of missed_days across available weeks) / (7 * weeks_available)

- volatility_index_4wk:
   High = frequent oscillation (most weeks show Mixed Pattern/Volatile OR large swings in win_days vs missed_days)
   Moderate = some oscillation but stable direction
   Low = stable pattern and stable tier

- recommitment_rate_4wk:
   Use only weeks where recommitment_signal is not "insufficient data" or "Not Applicable".
   Rate = % of evaluable weeks with recommitment_signal Strong/Moderate.

- controllable_stability_4wk:
   Stable = most_important_controllable repeats in majority of weeks
   Shifting = changes often but still within same theme
   Chaotic = changes every week with no continuity

- fatigue_load_indicator:
   High = rolling_missed_rate elevated + volatility high + reflection_quality_score trending down OR weekly_tier_trend declining
   Moderate = mixed indicators
   Low = stable/improving with low missed rate

If insufficient data:
Set longitudinal_metrics fields to "insufficient data".

Do not speculate.
All longitudinal_metrics fields must be computed before JSON construction
and must follow VF_Longitudinal_Metrics_Rules.txt exactly.

If conflict occurs between prompt wording and VF_Longitudinal_Metrics_Rules.txt,
the rules file governs.
============================================================
INTERPRETATION SCOPE
============================================================

Use:
- Win the Day official scoring interpretation
- Bullseye Method ring definitions
- Reflection Quality Scale (1–4)
- Growth Phase classification model
- Tracking & Reporting performance indicators
- VF_Interpretation_JSON_Rules.txt deterministic rules

Interpret behavior, habit consistency, controllable focus, identity alignment,
recommitment patterns, baseline alignment, and longitudinal trajectory only.

Remain fully non-clinical and performance-based.

============================================================
OUTPUT REQUIREMENTS
============================================================

Return JSON only.

Follow the REQUIRED OUTPUT SCHEMA exactly.
Do not omit any field.

If any field cannot be determined from the provided data,
use "insufficient data".

Do not output commentary outside JSON.

Read all source and provided documents in their entirety before generating output.

Return JSON only as a downloadable .txt document.